process {
    
    withLabel: python_based {
        container = "quay.io/microbiome-informatics/mag-assembly-linking"
    }

    withName: POSTPROCESSING {
        publishDir = [
            path: { "${params.output_path}" },
            mode: 'copy'
        ]
    }
}