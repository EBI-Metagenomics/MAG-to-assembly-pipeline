process {

    withName: FINALISE_OUTPUT {
        publishDir = [
            path: { "${params.output_path}" },
            mode: 'copy'
        ]
        cpus   = { 1                    }
        memory = { 15.GB * task.attempt }
        time   = { 4.h  * task.attempt  }
    }
}