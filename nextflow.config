// required files
params.gut_mapping = "$projectDir/data/known_and_found_gut_genomes.tsv"
params.empty_file = "$projectDir/data/dummy_file.tsv"

// names for generated intermidiate files 
params.input_accessions = 'input_accessions.tsv'
params.catalogue_metadata = 'all_catalog_metadata.tsv'

// user input args
// params.n_tasks = 100
params.portion_size = 10
params.previous_table = null
params.processed_acc = null
params.output_path = "$PWD"
params.external_input = null


profiles {
    codon_slurm { 
        includeConfig 'conf/codon_slurm.config' 
    }
} 

includeConfig 'conf/modules.config'